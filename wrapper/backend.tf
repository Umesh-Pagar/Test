terraform {
  backend "azurerm" {
    # resource_group_name   = "RG-UmeshPagar"
    # storage_account_name  = "stgtestdev01"
    # container_name        = "terraform-state-dev-container"
    # key                   = "terraform-dev.tfstate"
  }
}